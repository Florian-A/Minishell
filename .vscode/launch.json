{
    "version": "1.0.0",
    "configurations": [
        {
            "type": "lldb",
            "request": "launch",
            "name": "minishell",
            "program": "${workspaceFolder}/minishell",
            "args": ["DEBUG=1"],
            "cwd": "${workspaceFolder}",
            "preLaunchTask": "build",
            "env": {
                "PATH": "${env:PATH}"
            },
        },
        {
            "type": "lldb",
            "request": "launch",
            "name": "utest",
            "program": "${workspaceFolder}/utest",
            "args": [],
            "cwd": "${workspaceFolder}",
            "preLaunchTask": "build",
            "env": {
                "PATH": "${env:PATH}"
            },
        }
    ]
}